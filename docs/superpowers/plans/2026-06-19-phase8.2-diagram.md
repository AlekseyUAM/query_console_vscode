# Кнопка «Диаграмма» (8.2) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в шапку конструктора кнопку-дропдаун «Диаграмма ▾» с 4 типами mermaid-диаграмм текущего запроса, открывающихся во встроенном Markdown-preview VS Code.

**Architecture:** Чистое ядро `mermaidDiagram.ts` (модель → mermaid-текст, TDD, никогда не бросает) импортируется в webview, как `generateBatch`. Webview генерит текст и шлёт хосту новое сообщение `showDiagram`; тонкий хост (`diagramPreview.ts`) пишет временный markdown и открывает превью, при отсутствии расширения `bierner.markdown-mermaid` предлагает установку.

**Tech Stack:** TypeScript, React (webview), VS Code Extension API, vitest.

## Global Constraints

- Ядро `src/core/query/*` — **pure-TS**: без импортов `vscode`, React, файловой системы.
- `buildDiagram` **никогда не бросает**: на пустом/некорректном вводе или неизвестном `kind` возвращает валидный mermaid с плейсхолдер-узлом «Пустой запрос» (паттерн `commentBinder`).
- Новый код **вне** канонического конвейера: `oracleGolden.test.ts` и `corpusRegression.test.ts` не затрагиваются и обязаны остаться байт-в-байт.
- Тесты ядра — vitest, файл `test/unit/mermaidDiagram.test.ts`, импорт `import { describe, it, expect } from 'vitest'`.
- Команда прогона: `npm run test:unit` (vitest run). Точечно: `npx vitest run test/unit/mermaidDiagram.test.ts`.
- Расширение НЕ добавляется в `extensionDependencies`/recommendations — только runtime-prompt.
- Коммиты после каждой задачи; стиль сообщений как в репозитории (`feat(phase8.2): …`).

---

## Структура файлов

| Файл | Ответственность |
|------|-----------------|
| `src/core/query/mermaidDiagram.ts` | новый: `DiagramKind`, `buildDiagram`, 4 генератора, хелперы экранирования/подписей |
| `test/unit/mermaidDiagram.test.ts` | новый: golden-вывод 4 типов + краевые случаи |
| `src/shared/messages.ts` | новый `WebviewMsg` `showDiagram` |
| `src/extension/diagramPreview.ts` | новый: `openDiagram` (временный файл + prompt установки + превью) |
| `src/extension/panel.ts` | обработчик `showDiagram` |
| `src/webview/App.tsx` | кнопка-дропдаун «Диаграмма ▾» в `cacheToolbar` |

---

## Task 1: Ядро — скелет, диспетчер, экранирование, плейсхолдер

**Files:**
- Create: `src/core/query/mermaidDiagram.ts`
- Test: `test/unit/mermaidDiagram.test.ts`

**Interfaces:**
- Consumes: `BatchDocument` (`./batchModel`), `QueryDocument` (`./unionModel`), `QueryModel`/`SelectedTable`/`Join`/`FieldRef`/`OrderField`/`TotalField`/`Condition` (`./queryModel`), `orderedSelectElements`/`elementAlias` (`./unionModel`).
- Produces: `export type DiagramKind = 'packageFlow' | 'joins' | 'unions' | 'fields'`; `export function buildDiagram(batch: BatchDocument, kind: DiagramKind, activeIndex: number): string`. В этой задаче 4 генератора (`packageFlowDiagram`/`joinsDiagram`/`unionsDiagram`/`fieldsDiagram`) — приватные **заглушки**, возвращающие `placeholder()`; реальные реализации добавляют Tasks 2–5.

- [ ] **Step 1: Написать падающий тест**

В `test/unit/mermaidDiagram.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { buildDiagram } from '../../src/core/query/mermaidDiagram';
import type { BatchDocument } from '../../src/core/query/batchModel';

const emptyBatch: BatchDocument = { members: [] };

describe('buildDiagram — диспетчер и устойчивость', () => {
  it('пустой пакет → плейсхолдер, не бросает', () => {
    const out = buildDiagram(emptyBatch, 'packageFlow', 0);
    expect(out).toContain('graph');
    expect(out).toContain('Пустой запрос');
  });

  it('неизвестный kind → плейсхолдер', () => {
    // @ts-expect-error проверяем defensive-ветку
    const out = buildDiagram(emptyBatch, 'nope', 0);
    expect(out).toContain('Пустой запрос');
  });

  it('activeIndex за пределами пакета → плейсхолдер', () => {
    const out = buildDiagram(emptyBatch, 'joins', 5);
    expect(out).toContain('Пустой запрос');
  });
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts`
Expected: FAIL — `buildDiagram` не определён / модуль не найден.

- [ ] **Step 3: Реализовать скелет**

Создать `src/core/query/mermaidDiagram.ts`:

```ts
import type { BatchDocument } from './batchModel';
import type { QueryDocument } from './unionModel';
import type {
  QueryModel, SelectedTable, Join, FieldRef, OrderField, TotalField, Condition,
} from './queryModel';
import { orderedSelectElements, elementAlias } from './unionModel';

export type DiagramKind = 'packageFlow' | 'joins' | 'unions' | 'fields';

/** Экранирование подписи узла/ребра: убираем ломающие mermaid символы. */
function esc(label: string): string {
  const s = String(label ?? '')
    .replace(/"/g, "'")
    .replace(/[\r\n]+/g, ' ')
    .replace(/[[\]{}|<>]/g, ' ')
    .trim();
  return s.length ? s : ' ';
}

function node(id: string, label: string): string {
  return `  ${id}["${esc(label)}"]`;
}

function edge(from: string, to: string, label?: string): string {
  return label != null && label.length
    ? `  ${from} -->|"${esc(label)}"| ${to}`
    : `  ${from} --> ${to}`;
}

function placeholder(): string {
  return 'graph TD\n' + node('n0', 'Пустой запрос');
}

/** Модель «головного» участника запроса пакета (для joins/fields). */
function headModel(doc: QueryDocument): QueryModel | undefined {
  return doc.members[0]?.model;
}

export function buildDiagram(
  batch: BatchDocument,
  kind: DiagramKind,
  activeIndex: number,
): string {
  try {
    const active = batch.members[activeIndex];
    switch (kind) {
      case 'packageFlow':
        return packageFlowDiagram(batch);
      case 'joins': {
        const m = active && headModel(active);
        return m ? joinsDiagram(m) : placeholder();
      }
      case 'unions':
        return active ? unionsDiagram(active) : placeholder();
      case 'fields': {
        const m = active && headModel(active);
        return m ? fieldsDiagram(m) : placeholder();
      }
      default:
        return placeholder();
    }
  } catch {
    return placeholder();
  }
}

// --- генераторы (заглушки; реальные реализации — Tasks 2–5) ---

function packageFlowDiagram(_batch: BatchDocument): string {
  return placeholder();
}

function joinsDiagram(_model: QueryModel): string {
  return placeholder();
}

function unionsDiagram(_doc: QueryDocument): string {
  return placeholder();
}

function fieldsDiagram(_model: QueryModel): string {
  return placeholder();
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts`
Expected: PASS (3 теста).

- [ ] **Step 5: Коммит**

```bash
git add src/core/query/mermaidDiagram.ts test/unit/mermaidDiagram.test.ts
git commit -m "feat(phase8.2): ядро диаграмм — диспетчер и плейсхолдер (8.2.1)"
```

---

## Task 2: Генератор `packageFlow` — поток ВТ по пакету

**Files:**
- Modify: `src/core/query/mermaidDiagram.ts` (заменить заглушку `packageFlowDiagram` + добавить хелпер `packageNodeLabel`)
- Test: `test/unit/mermaidDiagram.test.ts` (добавить describe-блок)

**Interfaces:**
- Consumes: `BatchDocument.members: QueryDocument[]`; у головной модели — `queryType` (`'createTemp'|'appendTemp'|'dropTemp'|'select'`), `tempTableName`, `tables[*].fullName`.
- Produces: `packageFlowDiagram(batch)` возвращает `graph TD` с узлами `q0..qN` и рёбрами «производитель ВТ → потребитель» (подпись ребра = имя ВТ).

- [ ] **Step 1: Написать падающий тест**

Добавить в `test/unit/mermaidDiagram.test.ts`:

```ts
import type { QueryModel } from '../../src/core/query/queryModel';

function q(model: Partial<QueryModel>): { members: { name: string; distinct: boolean; model: QueryModel }[] } {
  const full: QueryModel = { tables: [], fields: [], ...model };
  return { members: [{ name: '', distinct: false, model: full }] };
}

describe('packageFlow', () => {
  it('ПОМЕСТИТЬ ВТ → потребитель: узлы и ребро с именем ВТ', () => {
    const batch: BatchDocument = {
      members: [
        q({ queryType: 'createTemp', tempTableName: 'ВТТовары', tables: [{ id: 'a', fullName: 'Справочник.Номенклатура' }] }),
        q({ queryType: 'select', tables: [{ id: 'b', fullName: 'ВТТовары' }] }),
      ],
    };
    const out = buildDiagram(batch, 'packageFlow', 0);
    expect(out.startsWith('graph TD')).toBe(true);
    expect(out).toContain('ПОМЕСТИТЬ ВТТовары');
    expect(out).toContain('Запрос 2');
    expect(out).toContain('q0 -->|"ВТТовары"| q1');
  });

  it('УНИЧТОЖИТЬ ВТ — отдельный узел без исходящих рёбер', () => {
    const batch: BatchDocument = {
      members: [q({ queryType: 'dropTemp', tempTableName: 'ВТТовары' })],
    };
    const out = buildDiagram(batch, 'packageFlow', 0);
    expect(out).toContain('УНИЧТОЖИТЬ ВТТовары');
    expect(out).not.toContain('-->');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t packageFlow`
Expected: FAIL — выводится плейсхолдер, нет `q0 -->`.

- [ ] **Step 3: Реализовать**

Заменить заглушку `packageFlowDiagram` и добавить хелпер:

```ts
function packageNodeLabel(doc: QueryDocument, i: number): string {
  const m = headModel(doc);
  const qt = m?.queryType;
  const name = m?.tempTableName;
  if ((qt === 'createTemp' || qt === 'appendTemp') && name) {
    return `${i + 1}. ПОМЕСТИТЬ ${name}`;
  }
  if (qt === 'dropTemp' && name) {
    return `${i + 1}. УНИЧТОЖИТЬ ${name}`;
  }
  return `Запрос ${i + 1}`;
}

function packageFlowDiagram(batch: BatchDocument): string {
  if (!batch.members.length) return placeholder();
  const lines = ['graph TD'];

  // Имя ВТ (в верхнем регистре) → индекс запроса-производителя.
  const producer = new Map<string, number>();
  batch.members.forEach((doc, i) => {
    const m = headModel(doc);
    if ((m?.queryType === 'createTemp' || m?.queryType === 'appendTemp') && m.tempTableName) {
      producer.set(m.tempTableName.toUpperCase(), i);
    }
    lines.push(node(`q${i}`, packageNodeLabel(doc, i)));
  });

  // Ребро на каждую таблицу-потребителя, чьё имя совпало с произведённой ВТ.
  batch.members.forEach((doc, i) => {
    const seen = new Set<string>();
    for (const um of doc.members) {
      for (const t of um.model.tables) {
        const key = (t.fullName ?? '').toUpperCase();
        const p = producer.get(key);
        if (p == null || p === i) continue;
        const ek = `${p}->${i}:${key}`;
        if (seen.has(ek)) continue;
        seen.add(ek);
        lines.push(edge(`q${p}`, `q${i}`, t.fullName));
      }
    }
  });

  return lines.join('\n');
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t packageFlow`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add src/core/query/mermaidDiagram.ts test/unit/mermaidDiagram.test.ts
git commit -m "feat(phase8.2): диаграмма потока пакета (ВТ) (8.2.2)"
```

---

## Task 3: Генератор `joins` — схема соединений активного запроса

**Files:**
- Modify: `src/core/query/mermaidDiagram.ts` (заменить заглушку `joinsDiagram` + хелперы `tableLabel`/`joinKindLabel`/`joinConditionText`)
- Test: `test/unit/mermaidDiagram.test.ts`

**Interfaces:**
- Consumes: `QueryModel.tables: SelectedTable[]` (`id`, `fullName`, `alias?`), `QueryModel.joins?: Join[]` (`leftTableId`, `rightTableId`, `leftAll`, `rightAll`, `custom`, `leftPath`, `operator`, `rightPath`, `expression`).
- Produces: `joinsDiagram(model)` → `graph LR` с узлами `t0..tN` (по `tables`) и рёбрами по `joins` (подпись = тип соединения + условие).

- [ ] **Step 1: Написать падающий тест**

```ts
import type { Join, SelectedTable } from '../../src/core/query/queryModel';

describe('joins', () => {
  it('ЛЕВОЕ соединение с простым условием', () => {
    const tables: SelectedTable[] = [
      { id: 'A', fullName: 'Справочник.Заказы', alias: 'Заказы' },
      { id: 'B', fullName: 'Справочник.Контрагенты', alias: 'Контр' },
    ];
    const joins: Join[] = [{
      leftTableId: 'A', rightTableId: 'B', leftAll: true, rightAll: false,
      custom: false, leftPath: 'Контрагент', operator: '=', rightPath: 'Ссылка',
    }];
    const out = buildDiagram({ members: [{ members: [{ name: '', distinct: false, model: { tables, fields: [], joins } } ] }] } as any, 'joins', 0);
    expect(out.startsWith('graph LR')).toBe(true);
    expect(out).toContain('Справочник.Заказы');
    expect(out).toContain('t0 -->|"ЛЕВОЕ: Контрагент = Ссылка"| t1');
  });

  it('нет таблиц → плейсхолдер', () => {
    const out = buildDiagram({ members: [{ members: [{ name: '', distinct: false, model: { tables: [], fields: [] } }] }] } as any, 'joins', 0);
    expect(out).toContain('Пустой запрос');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t joins`
Expected: FAIL — плейсхолдер вместо `graph LR`.

- [ ] **Step 3: Реализовать**

```ts
function tableLabel(t: SelectedTable): string {
  if (t.alias && t.alias !== t.fullName) return `${t.fullName} (${t.alias})`;
  return t.fullName || t.alias || 'Таблица';
}

function joinKindLabel(j: Join): string {
  if (j.leftAll && j.rightAll) return 'ПОЛНОЕ';
  if (j.leftAll) return 'ЛЕВОЕ';
  if (j.rightAll) return 'ПРАВОЕ';
  return 'ВНУТРЕННЕЕ';
}

function joinConditionText(j: Join): string {
  if (j.expression) return j.expression;
  if (j.leftPath && j.rightPath) return `${j.leftPath} ${j.operator ?? '='} ${j.rightPath}`;
  return '';
}

function joinsDiagram(model: QueryModel): string {
  if (!model.tables.length) return placeholder();
  const lines = ['graph LR'];
  const idOf = new Map<string, string>();
  model.tables.forEach((t, i) => {
    const nid = `t${i}`;
    idOf.set(t.id, nid);
    lines.push(node(nid, tableLabel(t)));
  });
  for (const j of model.joins ?? []) {
    const l = idOf.get(j.leftTableId);
    const r = idOf.get(j.rightTableId);
    if (!l || !r) continue;
    const cond = joinConditionText(j);
    const label = cond ? `${joinKindLabel(j)}: ${cond}` : joinKindLabel(j);
    lines.push(edge(l, r, label));
  }
  return lines.join('\n');
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t joins`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add src/core/query/mermaidDiagram.ts test/unit/mermaidDiagram.test.ts
git commit -m "feat(phase8.2): диаграмма схемы соединений (8.2.3)"
```

---

## Task 4: Генератор `unions` — участники объединения активного запроса

**Files:**
- Modify: `src/core/query/mermaidDiagram.ts` (заменить заглушку `unionsDiagram`)
- Test: `test/unit/mermaidDiagram.test.ts`

**Interfaces:**
- Consumes: `QueryDocument.members: UnionMember[]` (`name: string`, `distinct: boolean`). `distinct===true` → `ОБЪЕДИНИТЬ`; `false` → `ОБЪЕДИНИТЬ ВСЕ`. `distinct` описывает оператор ПЕРЕД участником, поэтому подпись ставится у участников с индексом > 0.
- Produces: `unionsDiagram(doc)` → `graph TD`: узлы `u0..uN` → общий узел `result` («Результат»).

- [ ] **Step 1: Написать падающий тест**

```ts
describe('unions', () => {
  it('три участника ОБЪЕДИНИТЬ ВСЕ сходятся в результат', () => {
    const doc = {
      members: [
        { name: 'Заказы', distinct: false, model: { tables: [], fields: [] } },
        { name: 'Возвраты', distinct: false, model: { tables: [], fields: [] } },
        { name: 'Списания', distinct: true, model: { tables: [], fields: [] } },
      ],
    };
    const out = buildDiagram({ members: [doc] } as any, 'unions', 0);
    expect(out.startsWith('graph TD')).toBe(true);
    expect(out).toContain('u0 --> result');
    expect(out).toContain('u1 -->|"ОБЪЕДИНИТЬ ВСЕ"| result');
    expect(out).toContain('u2 -->|"ОБЪЕДИНИТЬ"| result');
    expect(out).toContain('Результат');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t unions`
Expected: FAIL — плейсхолдер.

- [ ] **Step 3: Реализовать**

```ts
function unionsDiagram(doc: QueryDocument): string {
  if (!doc.members.length) return placeholder();
  const lines = ['graph TD'];
  doc.members.forEach((um, i) => {
    const nid = `u${i}`;
    lines.push(node(nid, um.name || `Запрос ${i + 1}`));
    if (i === 0) {
      lines.push(edge(nid, 'result'));
    } else {
      lines.push(edge(nid, 'result', um.distinct ? 'ОБЪЕДИНИТЬ' : 'ОБЪЕДИНИТЬ ВСЕ'));
    }
  });
  lines.push(node('result', 'Результат'));
  return lines.join('\n');
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t unions`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add src/core/query/mermaidDiagram.ts test/unit/mermaidDiagram.test.ts
git commit -m "feat(phase8.2): диаграмма объединений (8.2.4)"
```

---

## Task 5: Генератор `fields` — дерево структуры активного запроса

**Files:**
- Modify: `src/core/query/mermaidDiagram.ts` (заменить заглушку `fieldsDiagram` + хелперы подписей секций)
- Test: `test/unit/mermaidDiagram.test.ts`

**Interfaces:**
- Consumes: `orderedSelectElements(model)` + `elementAlias(el, model)` (поля выборки); `model.grouping?.groupFields: FieldRef[]`; `model.conditions?: Condition[]`; `model.order?.fields: OrderField[]`; `model.totals?.totalFields: TotalField[]`. У `FieldRef`/`OrderField`/`TotalField`: `path`, `expression?`. У `OrderField` ещё `direction` (`'asc'|'desc'`), `selectAlias?`. У `Condition`: `path?`, `operator?`, `param?`, `expression?`.
- Produces: `fieldsDiagram(model)` → `graph TD` с корнем `root` («Запрос») и узлами-секциями (Поля/Группировка/Условия/Порядок/Итоги); пустые секции опускаются; если непусто ни одной секции — плейсхолдер.

- [ ] **Step 1: Написать падающий тест**

```ts
describe('fields', () => {
  it('секции Поля и Порядок с листьями', () => {
    const model = {
      tables: [{ id: 'A', fullName: 'Справочник.Заказы', alias: 'Заказы' }],
      fields: [{ tableId: 'A', path: 'Ссылка', alias: 'Ссылка' }],
      order: { auto: false, fields: [{ tableId: 'A', path: 'Дата', direction: 'desc' }] },
    };
    const out = buildDiagram({ members: [{ members: [{ name: '', distinct: false, model }] }] } as any, 'fields', 0);
    expect(out.startsWith('graph TD')).toBe(true);
    expect(out).toContain('Запрос');
    expect(out).toContain('Поля');
    expect(out).toContain('Порядок');
    expect(out).toContain('Дата УБЫВ');
  });

  it('запрос без секций → плейсхолдер', () => {
    const out = buildDiagram({ members: [{ members: [{ name: '', distinct: false, model: { tables: [], fields: [] } }] }] } as any, 'fields', 0);
    expect(out).toContain('Пустой запрос');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t fields`
Expected: FAIL — плейсхолдер вместо дерева.

- [ ] **Step 3: Реализовать**

```ts
function fieldRefLabel(f: FieldRef): string {
  return f.expression ?? f.path ?? 'поле';
}

function conditionLabel(c: Condition): string {
  if (c.expression) return c.expression;
  if (c.path) return `${c.path} ${c.operator ?? '='} ${c.param ?? ''}`.trim();
  return 'условие';
}

function orderLabel(o: OrderField): string {
  const base = o.expression ?? o.selectAlias ?? o.path ?? 'поле';
  return o.direction === 'desc' ? `${base} УБЫВ` : base;
}

function totalLabel(t: TotalField): string {
  return t.expression ?? t.path ?? 'итог';
}

function fieldsDiagram(model: QueryModel): string {
  const lines = ['graph TD', node('root', 'Запрос')];
  let counter = 0;

  const addSection = (title: string, items: string[]): void => {
    if (!items.length) return;
    const sid = `s${counter++}`;
    lines.push(node(sid, title));
    lines.push(edge('root', sid));
    for (const it of items) {
      const lid = `l${counter++}`;
      lines.push(node(lid, it));
      lines.push(edge(sid, lid));
    }
  };

  addSection('Поля', orderedSelectElements(model).map(el => elementAlias(el, model)));
  addSection('Группировка', (model.grouping?.groupFields ?? []).map(fieldRefLabel));
  addSection('Условия', (model.conditions ?? []).map(conditionLabel));
  addSection('Порядок', (model.order?.fields ?? []).map(orderLabel));
  addSection('Итоги', (model.totals?.totalFields ?? []).map(totalLabel));

  if (counter === 0) return placeholder();
  return lines.join('\n');
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/mermaidDiagram.test.ts -t fields`
Expected: PASS.

- [ ] **Step 5: Тест экранирования + полный прогон ядра**

Добавить в тест-файл:

```ts
describe('экранирование', () => {
  it('кавычки и скобки в подписи не ломают mermaid', () => {
    const model = {
      tables: [{ id: 'A', fullName: 'Справочник."Странное[имя]"', alias: 'A' }],
      fields: [],
    };
    const out = buildDiagram({ members: [{ members: [{ name: '', distinct: false, model }] }] } as any, 'joins', 0);
    expect(out).not.toMatch(/\[\s*"[^"]*"[^\]]*[[\]"][^\]]*"\s*\]/); // нет вложенных "/[/]
    expect(out).toContain("Справочник.'Странное имя'");
  });
});
```

Run: `npx vitest run test/unit/mermaidDiagram.test.ts`
Expected: PASS (все блоки).

- [ ] **Step 6: Коммит**

```bash
git add src/core/query/mermaidDiagram.ts test/unit/mermaidDiagram.test.ts
git commit -m "feat(phase8.2): диаграмма полей/структуры + экранирование (8.2.5)"
```

---

## Task 6: Контракт `showDiagram` + хост (превью и prompt установки)

**Files:**
- Modify: `src/shared/messages.ts`
- Create: `src/extension/diagramPreview.ts`
- Modify: `src/extension/panel.ts` (новая ветка обработчика)

**Interfaces:**
- Consumes: `DiagramKind` из `../core/query/mermaidDiagram` (тип для сообщения).
- Produces: `WebviewMsg` `{ type: 'showDiagram'; kind: DiagramKind; mermaid: string; title: string }`; `export async function openDiagram(context: vscode.ExtensionContext, kind: string, title: string, mermaid: string): Promise<void>`.

- [ ] **Step 1: Добавить тип сообщения**

В `src/shared/messages.ts` — импорт и новый вариант `WebviewMsg`:

```ts
import type { DiagramKind } from '../core/query/mermaidDiagram';
```

В объединение `WebviewMsg` добавить последней строкой:

```ts
  | { type: 'showDiagram'; kind: DiagramKind; mermaid: string; title: string };
```

(перенести `;` со старой последней строки `{ type: 'refreshCache' }` на новую).

- [ ] **Step 2: Реализовать `openDiagram`**

Создать `src/extension/diagramPreview.ts`:

```ts
import * as vscode from 'vscode';

const MERMAID_EXT_ID = 'bierner.markdown-mermaid';

/** Пишет markdown с mermaid-блоком во временный файл и открывает встроенное превью.
 *  Если расширение для рендера mermaid не установлено — предлагает установку. */
export async function openDiagram(
  context: vscode.ExtensionContext,
  kind: string,
  title: string,
  mermaid: string,
): Promise<void> {
  const md = `# ${title}\n\n\`\`\`mermaid\n${mermaid}\n\`\`\`\n`;

  const dir = context.globalStorageUri;
  await vscode.workspace.fs.createDirectory(dir);
  const fileUri = vscode.Uri.joinPath(dir, `diagram-${kind}.md`);
  await vscode.workspace.fs.writeFile(fileUri, Buffer.from(md, 'utf8'));

  if (!vscode.extensions.getExtension(MERMAID_EXT_ID)) {
    void vscode.window
      .showInformationMessage(
        'Для красивого отображения диаграммы установите расширение Markdown Preview Mermaid Support.',
        'Установить',
      )
      .then(choice => {
        if (choice === 'Установить') {
          void vscode.commands.executeCommand(
            'workbench.extensions.installExtension',
            MERMAID_EXT_ID,
          );
        }
      });
  }

  await vscode.commands.executeCommand('markdown.showPreview', fileUri);
}
```

- [ ] **Step 3: Подключить обработчик в `panel.ts`**

Добавить импорт рядом с прочими импортами `src/extension/panel.ts`:

```ts
import { openDiagram } from './diagramPreview';
```

В цепочке `onDidReceiveMessage` после ветки `refreshCache` (перед закрытием цепочки) добавить:

```ts
    } else if (msg.type === 'showDiagram') {
      await openDiagram(context, msg.kind, msg.title, msg.mermaid);
```

(`context` доступен в области `createPanel`/обработчика — тот же, что передаётся при создании панели; если имя параметра иное, использовать фактическое.)

- [ ] **Step 4: Проверить компиляцию и весь юнит-набор**

Run: `npx tsc --noEmit -p tsconfig.json && npm run test:unit`
Expected: компиляция без ошибок; все тесты зелёные; `oracleGolden`/`corpusRegression` без изменений.

- [ ] **Step 5: Коммит**

```bash
git add src/shared/messages.ts src/extension/diagramPreview.ts src/extension/panel.ts
git commit -m "feat(phase8.2): сообщение showDiagram и открытие превью (8.2.6)"
```

---

## Task 7: UI — кнопка-дропдаун «Диаграмма ▾» в шапке

**Files:**
- Modify: `src/webview/App.tsx`

**Interfaces:**
- Consumes: `buildDiagram`/`DiagramKind` из `../core/query/mermaidDiagram`; `assembleBatch(state)` (уже импортирован); `state.activeQuery` (индекс активного запроса пакета); `postToHost`.
- Produces: пункт меню → `postToHost({ type: 'showDiagram', kind, mermaid, title })`.

- [ ] **Step 1: Импорты и список типов**

В начало `src/webview/App.tsx` добавить импорт:

```ts
import { buildDiagram, type DiagramKind } from '../core/query/mermaidDiagram';
```

Рядом с константой `BTN` добавить список пунктов меню:

```ts
const DIAGRAM_ITEMS: { kind: DiagramKind; label: string }[] = [
  { kind: 'packageFlow', label: 'Поток пакета' },
  { kind: 'joins', label: 'Схема соединений' },
  { kind: 'unions', label: 'Объединения' },
  { kind: 'fields', label: 'Поля/структура' },
];
```

- [ ] **Step 2: Состояние и обработчик внутри `App`**

Рядом с прочими `useState` добавить:

```ts
  const [diagramOpen, setDiagramOpen] = useState(false);
```

Рядом с `handleRefreshCache` добавить:

```ts
  function handleDiagram(kind: DiagramKind, label: string) {
    setDiagramOpen(false);
    const mermaid = buildDiagram(assembleBatch(state), kind, state.activeQuery);
    postToHost({ type: 'showDiagram', kind, mermaid, title: `Диаграмма: ${label}` });
  }
```

- [ ] **Step 3: Кнопка-дропдаун в `cacheToolbar`**

В `cacheToolbar`, сразу после `</button>` кнопки «Обновить кэш» (до блока `typeof refreshState === 'object'`), вставить:

```tsx
      <div style={{ position: 'relative' }}>
        <button style={BTN} onClick={() => setDiagramOpen(o => !o)}>
          Диаграмма ▾
        </button>
        {diagramOpen && (
          <div
            style={{
              position: 'absolute', top: '100%', left: 0, zIndex: 50, marginTop: 2,
              background: 'var(--vscode-menu-background, #252526)',
              border: '1px solid var(--vscode-menu-border, #454545)', borderRadius: 2,
              minWidth: 160,
            }}
          >
            {DIAGRAM_ITEMS.map(item => (
              <div
                key={item.kind}
                onClick={() => handleDiagram(item.kind, item.label)}
                style={{
                  padding: '4px 12px', fontSize: 12, cursor: 'pointer',
                  color: 'var(--vscode-menu-foreground, #ccc)', whiteSpace: 'nowrap',
                }}
                onMouseEnter={e => (e.currentTarget.style.background = 'var(--vscode-menu-selectionBackground, #094771)')}
                onMouseLeave={e => (e.currentTarget.style.background = 'transparent')}
              >
                {item.label}
              </div>
            ))}
          </div>
        )}
      </div>
```

- [ ] **Step 4: Проверка компиляции и сборки webview**

Run: `npx tsc --noEmit -p tsconfig.json && npm run test:unit`
Expected: без ошибок типов; тесты зелёные.

(Визуальную проверку — открыть конструктор, нажать «Диаграмма ▾», выбрать тип, убедиться, что открылось превью — выполнить вручную после прогона.)

- [ ] **Step 5: Коммит**

```bash
git add src/webview/App.tsx
git commit -m "feat(phase8.2): кнопка-дропдаун «Диаграмма» в шапке конструктора (8.2.7)"
```

---

## Task 8: Документация фазы

**Files:**
- Modify: `docs/PHASE_8.md` (добавить раздел «Подфаза 2»)
- Modify: `docs/ROADMAP.md` (отметить 8.2 в описании Фазы 8)

**Interfaces:** нет (документация).

- [ ] **Step 1: Описать подфазу 8.2 в `docs/PHASE_8.md`**

Добавить новый раздел после «Подфаза 1» — кратко: цель, 4 типа диаграмм, делегация рендера `bierner.markdown-mermaid`, файлы (`mermaidDiagram.ts`, `diagramPreview.ts`, сообщение `showDiagram`, кнопка в `App.tsx`), отметить, что оракул/корпус не затронуты.

- [ ] **Step 2: Обновить `docs/ROADMAP.md`**

В абзаце про Фазу 8 добавить предложение про подфазу 8.2 (кнопка «Диаграмма», mermaid-визуализация, делегация стороннему расширению).

- [ ] **Step 3: Финальный прогон всего набора**

Run: `npm run test:unit`
Expected: все тесты зелёные, включая `oracleGolden`/`corpusRegression` без изменений.

- [ ] **Step 4: Коммит**

```bash
git add docs/PHASE_8.md docs/ROADMAP.md
git commit -m "docs(phase8.2): описание подфазы кнопки «Диаграмма»"
```

---

## Self-Review

**Spec coverage:**
- 4 типа диаграмм → Tasks 2–5 ✓
- Делегация рендера + prompt установки + временный файл + `markdown.showPreview` → Task 6 ✓
- Дропдаун в шапке, одна выбранная диаграмма, активный запрос → Task 7 ✓
- Ядро никогда не бросает / плейсхолдер / экранирование → Task 1 + Task 5 ✓
- Оракул/корпус не затронуты → проверяется в Tasks 6/7/8 (`npm run test:unit`) ✓
- Контракт `showDiagram` → Task 6 ✓

**Placeholder scan:** нет «TBD»/«позже»; весь код приведён дословно. Единственная оговорка — фактическое имя `context` в `panel.ts` (Task 6, Step 3): указано использовать реальное имя параметра, если отличается.

**Type consistency:** `DiagramKind`, `buildDiagram(batch, kind, activeIndex)`, `openDiagram(context, kind, title, mermaid)`, сообщение `{ type:'showDiagram'; kind; mermaid; title }` — согласованы между Tasks 1/6/7. Поля модели (`distinct`, `leftAll`/`rightAll`, `tempTableName`, `order.fields`, `totals.totalFields`, `grouping.groupFields`) выверены по `queryModel.ts`/`unionModel.ts`.
